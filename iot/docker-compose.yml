version: '3.8'

services:
  ns3-simulation:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./output:/ns-3-dev/output
    environment:
      - TERM=xterm-256color
    command: bash -c "./ns3 run scratch/sixlowpan_mqtt_simulation && python3 plot_metrics.py"